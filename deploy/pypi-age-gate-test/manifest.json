{
    "name": "pypi-age-gate-test",
    "description": "Gate uncached remote PyPI downloads by package version age (test scope). TODO: confirm repoKeys (virtual + remote) against the real PyPI test repos before first deploy — the remote key MUST be present or the gate never fires.",
    "filterCriteria": {
        "artifactFilterCriteria": {
            "repoKeys": ["pypi-appsec-test", "pypi-appsec-remote-test"]
        }
    },
    "secrets": {},
    "sourceCodePath": "./worker.ts",
    "action": "BEFORE_REMOTE_DOWNLOAD",
    "enabled": true,
    "debug": true,
    "projectKey": "",
    "application": "artifactory"
}
